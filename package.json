{
  "name": "alvesmaia-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "tipos": "tsc --noEmit -p api/tsconfig.json",
    "verificar": "npm run lint && npm run tipos && npm run build && npm test",
    "lint": "eslint .",
    "build": "npm run build --prefix api"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "eslint": "^9.39.5",
    "globals": "^17.12.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.70.0",
    "vitest": "^2.1.8"
  }
}
